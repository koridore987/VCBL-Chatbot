# ============================================
# VCBL Chatbot - 로컬 개발용 환경 변수 파일
# ============================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 수정하세요.
# 로컬 개발용 설정입니다. 프로덕션은 Google Cloud Secret Manager를 사용합니다.
# Docker Compose가 자동으로 이 파일을 읽어서 모든 컨테이너에 환경 변수를 주입합니다.

# ============================================
# 필수 설정 (반드시 수정 필요)
# ============================================

# OpenAI API 키 (필수)
OPENAI_API_KEY=your-openai-api-key-here

# 관리자 비밀번호 (필수)
ADMIN_PASSWORD=admin123!@#

# ============================================
# 데이터베이스 설정
# ============================================

# PostgreSQL 비밀번호
DB_PASSWORD=vcbl_dev_password_2024

# 데이터베이스 URL (로컬 개발용)
DATABASE_URL=postgresql://vcbl_user:vcbl_dev_password_2024@localhost:5433/vcbl_chatbot

# ============================================
# 보안 설정 (로컬 개발용)
# ============================================

# Flask 보안 키 (로컬 개발용 - 프로덕션은 Secret Manager 사용)
SECRET_KEY=dev-secret-key-for-local-development
JWT_SECRET_KEY=dev-jwt-secret-key-for-local-development

# JWT 토큰 만료일 (일)
JWT_ACCESS_TOKEN_EXPIRES_DAYS=7

# ============================================
# OpenAI 모델 설정
# ============================================

# 사용할 모델명
MODEL_NAME=gpt-4.1-mini

# 요약 트리거 토큰 수
SUMMARY_TRIGGER_TOKENS=3500

# 요청당 최대 토큰 수
MAX_TOKENS_PER_REQUEST=4000

# 출력 최대 토큰 수
MAX_TOKENS_OUTPUT=1000

# 일일 토큰 제한
DAILY_TOKEN_LIMIT=50000

# OpenAI API 타임아웃 (초)
OPENAI_TIMEOUT=30

# OpenAI 재시도 횟수
OPENAI_MAX_RETRIES=3

# ============================================
# 서버 설정 (로컬 개발용)
# ============================================

# Flask 환경 (로컬 개발용)
FLASK_ENV=development

# Flask 앱 파일명
FLASK_APP=run.py

# 서버 포트
PORT=8080

# 관리자 초기화 설정
INIT_ADMIN_ON_START=true

# ============================================
# CORS 설정
# ============================================

# 허용할 프론트엔드 도메인 (쉼표로 구분)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# Redis 설정 (로컬 개발용)
# ============================================

# Redis 연결 URL (Rate Limiting용 - 로컬 개발용)
# 도커 개발: redis://redis:6379/0
# 로컬 개발: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0
RATELIMIT_STORAGE_URL=redis://localhost:6379/0

# ============================================
# 로깅 설정
# ============================================

# 로그 레벨 (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO
